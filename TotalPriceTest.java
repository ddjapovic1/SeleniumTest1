// Generated by Selenium IDE
package Tests;
import org.junit.Test;
import org.junit.Before;
import org.junit.After;
import static org.junit.Assert.*;
import static org.hamcrest.CoreMatchers.is;
import static org.hamcrest.core.IsNot.not;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.firefox.FirefoxDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.remote.RemoteWebDriver;
import org.openqa.selenium.remote.DesiredCapabilities;
import org.openqa.selenium.Dimension;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.interactions.Actions;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.WebDriverWait;
import org.openqa.selenium.JavascriptExecutor;
import org.openqa.selenium.Alert;
import org.openqa.selenium.Keys;
import java.util.*;
import java.net.MalformedURLException;
import java.net.URL;
public class TotalPriceTest {
	private String
	exePath =
	"C:\\Users\\chromedriver.exe";
  private WebDriver driver;
  private Map<String, Object> vars;
  JavascriptExecutor js;
  @Before
  public void setUp() {
	  System.setProperty("webdriver.chrome.driver", exePath);
    driver = new ChromeDriver();
    js = (JavascriptExecutor) driver;
    vars = new HashMap<String, Object>();
  }
  @After
  public void tearDown() {
    driver.quit();
  }
  @Test
  public void totalPrice() {
    driver.get("https://www.sportivo.rs/");
    driver.manage().window().setSize(new Dimension(1296, 688));
    driver.findElements(By.className("cookieinfo-close")).get(0).click();
    driver.get("https://www.sportivo.rs/muska-obuca/muske-patike");
    driver.findElements(By.className("owl-item")).get(0).click();
    driver.findElements(By.className("radio")).get(0).click();
    driver.findElement(By.id("addToCartMD")).click();
    try {
 		Thread.sleep(5000);
 	} catch (InterruptedException e) {
 		// TODO Auto-generated catch block
 		e.printStackTrace();
 	}
    driver.findElement(By.id("fancybox-close")).click();
    try {
 		Thread.sleep(5000);
 	} catch (InterruptedException e) {
 		// TODO Auto-generated catch block
 		e.printStackTrace();
 	}
    driver.get("https://www.sportivo.rs/zenska-obuca/zenske-patike");
    driver.findElements(By.className("owl-item")).get(0).click();
    driver.findElements(By.className("radio")).get(0).click();
    driver.findElement(By.id("addToCartMD")).click();
    try {
 		Thread.sleep(5000);
 	} catch (InterruptedException e) {
 		// TODO Auto-generated catch block
 		e.printStackTrace();
 	}
    driver.findElement(By.id("fancybox-close")).click();
    try {
 		Thread.sleep(5000);
 	} catch (InterruptedException e) {
 		// TODO Auto-generated catch block
 		e.printStackTrace();
 	}
    
    driver.get("https://www.sportivo.rs/decja-obuca/decje-zimske-cipele");
    driver.findElements(By.className("owl-item")).get(0).click();
    driver.findElements(By.className("radio")).get(0).click();
    driver.findElement(By.id("addToCartMD")).click();
    try {
 		Thread.sleep(5000);
 	} catch (InterruptedException e) {
 		// TODO Auto-generated catch block
 		e.printStackTrace();
 	}
    driver.findElement(By.cssSelector(".showcart")).click();
    try {
 		Thread.sleep(5000);
 	} catch (InterruptedException e) {
 		// TODO Auto-generated catch block
 		e.printStackTrace();
 	}
    double suma = 0;
    String cena1 =  driver.findElement(By.id("subtotal_with_tax_0")).getText().replace(" RSD", "").replace(".", "").replace(",", ".");
    suma+= Double.parseDouble(cena1);
    System.out.println(cena1);
   String cena2 = driver.findElement(By.id("subtotal_with_tax_1")).getText().replace(" RSD", "").replace(".", "").replace(",", ".");
    suma+= Double.parseDouble(cena2);
    System.out.println(cena2);
   String cena3 = driver.findElement(By.id("subtotal_with_tax_2")).getText().replace(" RSD", "").replace(".", "").replace(",", ".");
    suma+= Double.parseDouble(cena3);
    System.out.println(cena3);
    String u = driver.findElement(By.id("sales_price")).getText().replace(" RSD", "").replace(".", "").replace(",", ".");
    double ukupno = Double.parseDouble(u);
    System.out.println(ukupno);
    System.out.println(u);
    assertEquals(suma,ukupno,0.01);
  }
}
